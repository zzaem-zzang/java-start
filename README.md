#  Java Basic

> Backend Developer로 성장하기 위한 Java Core 기초 다지기  
> 단순 문법 학습이 아닌, **로직 설계 능력과 코드 구조 이해**를 목표로 진행한 학습입니다.

---

##  Overview

이 레포지토리는 Java의 기본 문법을 학습하며  
“왜 이렇게 동작하는가?”에 집중하여 정리하였습니다.

기초 문법을 단순히 따라 치는 것이 아니라,

- 실행 구조를 이해하고
- 제어 흐름을 설계하고
- 코드 재사용 구조를 고민하며

백엔드 개발의 기반을 다졌습니다.

---

## 🛠 Tech Stack

- **Language**: Java
- **IDE**: IntelliJ IDEA
- **Environment**: JDK 17+

---

## 📚 Curriculum Structure

```
java-basic/
├── section02_hello_world
├── section03_variables
├── section04_operators
├── section05_conditional
├── section06_loop
├── section07_scope_casting
├── section08_practice
├── section09_array
└── section10_method
```

---

### 📂 section02_hello_world
- Java 프로그램의 기본 구조 이해
- `main` 메서드의 역할
- `System.out.println()`을 활용한 출력
- 컴파일 → 실행 과정 이해 (JVM 동작 흐름)

---

### 📂 section03_variables
- 변수 선언 및 초기화
- Primitive Type (`int`, `double`, `char`, `boolean` 등)
- 상수(`final`) 개념
- 자동 형변환 / 강제 형변환 이해

---

### 📂 section04_operators
- 산술 연산자 (`+`, `-`, `*`, `/`, `%`)
- 비교 연산자 (`==`, `!=`, `>`, `<`)
- 논리 연산자 (`&&`, `||`, `!`)
- 증감 연산자 (`++`, `--`)
- 연산자 우선순위 개념

---

### 📂 section05_conditional
- `if / else if / else` 조건 분기
- `switch` 문 활용
- 중첩 조건문 설계
- 논리식을 활용한 흐름 제어

---

### 📂 section06_loop
- `for` 반복문
- `while`, `do-while` 반복문
- 반복문 중첩
- `break` / `continue` 활용
- 반복을 통한 패턴 출력 및 누적 계산 로직 구현

---

### 📂 section07_scope_casting
- 변수의 스코프 (지역 변수 / 블록 범위)
- 메모리 관점에서의 변수 생명주기
- 형변환 심화 이해
- 잘못된 형변환 시 발생 가능한 문제 이해

---

### 📂 section08_practice
- 조건문 + 반복문 종합 문제 풀이
- 입력값 기반 로직 설계
- 문제 해결 중심 코드 작성 훈련
- 로직 디버깅 경험

---

### 📂 section09_array
- 1차원 배열 선언 및 초기화
- 배열 반복 처리
- 배열을 활용한 데이터 저장 및 탐색
- 2차원 배열 기초
- 인덱스 기반 접근 방식 이해

---

### 📂 section10_method
- 메서드 정의 및 호출
- 매개변수(Parameter)와 반환값(Return)
- 기능 단위 분리
- 코드 재사용 및 모듈화 설계